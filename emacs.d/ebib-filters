;; -*- mode: emacs-lisp -*-

;; Ebib filters file
;; Saved on 2014.08.18 17:42

(("QT+group"
  (and
   (contains keywords "trajec")
   (contains keywords "group")))
 ("QT"
  (contains keywords "trajec")))
