;; -*- mode: emacs-lisp -*-

;; Ebib filters file
;; Saved on 2014.09.22 13:09

(("QT"
  (contains keywords "trajec"))
 ("QT+group"
  (and
   (contains keywords "trajec")
   (contains keywords "group"))))
