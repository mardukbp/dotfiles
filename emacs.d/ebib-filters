;; -*- mode: emacs-lisp -*-

;; Ebib filters file
;; Saved on 2014.12.19 20:50

(("QT"
  (contains keywords "trajec"))
 ("QT+group"
  (and
   (contains keywords "trajec")
   (contains keywords "group"))))
