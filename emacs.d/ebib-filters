;; -*- mode: emacs-lisp -*-

;; Ebib filters file
;; Saved on 2014.04.02 15:17

(("QT+group"
  (and
   (contains keywords "trajec")
   (contains keywords "group")))
 ("QT"
  (contains keywords "trajec")))
